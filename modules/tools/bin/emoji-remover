#!/bin/bash
# Emoji Remover Tool - Module Interface

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
MODULE_DIR="$(dirname "$SCRIPT_DIR")"

# Load the tool
source "$MODULE_DIR/src/emoji-remover/core.sh"

# Execute main function with all arguments
main "$@"